{
  "name": "buyer",
  "version": "1.0.0",
  "scripts": {
    "full": "concurrently \"npm run dev --prefix frontend\" \"npm run start:dev --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}