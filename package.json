{
  "name": "buyer",
  "version": "1.0.0",
  "scripts": {
    "full": "concurrently \"npm run start --prefix backend\" \"npm run dev --prefix frontend\" "
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}