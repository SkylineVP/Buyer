{
  "name": "buyer",
  "version": "1.0.0",
  "scripts": {
    "full": "concurrently \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\" "
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  }
}